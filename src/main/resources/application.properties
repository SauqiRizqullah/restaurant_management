spring.application.name=challengeshop

# Database Configuration
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Sauqisan1!}
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:db_wmb}
# driver prostgres
spring.datasource.driver-class-name=org.postgresql.Driver
# buat table dari class
spring.jpa.hibernate.ddl-auto=update
# untuk tampilin SQL di terminal aja
spring.jpa.show-sql=true
# untuk tampilin SQL di terminal biar cantik
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.highlight_sql=true
## untuk timezone json
spring.jackson.time-zone=Asia/Jakarta
# untuk timezone database
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Jakarta

#server.error.include-stacktrace=never

# JWT Config
# kunci rahasi
shop.jwt.secret_key=${JWT_SECRET:YWt1dGFrdGF1YXBheWFuZ3RlcmphZGk=}
# yg mencetak jwt
shop.jwt.issuer=Sauqi
# ini expirednya, defaultnya kita buat 9 detik
shop.jwt.expirationInSecond=${JWT_EXPIRATION:28800000}

shop.superadmin.username=${USERNAME_SUPER_ADMIN:superadmin}
shop.superadmin.password=${PASSWORD_SUPER_ADMIN:password}

